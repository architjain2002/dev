#include <bits/stdc++.h>

typedef long long ll;
using namespace std;


class Solution{
    
private:

public:
    ll solve() {
        
    }
};

int main(){
    #ifndef ONLINE_JUDGE
        freopen("input.txt", "r", stdin);
        freopen("output.txt", "w", stdout);
    #endif

    int T;
    cin >> T;

    while(T--){

        

        Solution sol;
        ll ans = sol.solve();
        cout << ans << endl;
    }

    return 0;
}